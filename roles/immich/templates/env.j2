# Généré par Ansible — bye-google / Immich
# (mêmes variables que le example.env officiel d'Immich)
IMMICH_VERSION={{ immich_release if immich_release != 'latest' else 'release' }}

UPLOAD_LOCATION={{ data_root }}/immich/library
DB_DATA_LOCATION={{ data_root }}/immich/postgres

TZ={{ timezone }}

DB_PASSWORD={{ vault_immich_db_password }}
DB_USERNAME=postgres
DB_DATABASE_NAME=immich
