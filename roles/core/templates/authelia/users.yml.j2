# Généré par Ansible — bye-google / utilisateurs Authelia
users:
  {{ admin_user }}:
    disabled: false
    displayname: '{{ admin_user }}'
    password: '{{ vault_authelia_admin_password_hash }}'
    email: '{{ admin_email }}'
    groups:
      - admins
